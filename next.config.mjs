/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['prada-ten.vercel.app'],  // Only the domain name, no protocol or path
    },
  };
  
  export default nextConfig;
  